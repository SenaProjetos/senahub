// Stub do pacote `server-only` para o servidor custom (server.ts via tsx).
// O Next resolve o `server-only` real no grafo RSC; aqui é um no-op inofensivo
// porque o código já roda exclusivamente no servidor Node.
export {};
