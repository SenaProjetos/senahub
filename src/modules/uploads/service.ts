import "server-only";

export { destinoArquivo, extensao, EXT_PACOTE_A, type PacoteAlvo, type PacoteDestino } from "./destino";

export {
  TAMANHO_MAX,
  TAMANHO_MAX_LABEL,
  TAMANHO_MAX_BACKUP,
  TAMANHO_MAX_BACKUP_LABEL,
  limiteDoPacote,
  limiteLabelDoPacote,
} from "./limites";
